<!DOCTYPE html>
<html>
<head>
  <title>AI Chatbot</title>
  <style>
    body { 
      font-family: Arial; 
      text-align: center; 
      background: #121212; 
      color: #f0f0f0; 
    }
    header {
      text-align: center;
      margin-top: 20px;
    }
    header img {
      width: 100px;
      border-radius: 10px;
    }
    #chat { 
      border: 1px solid #333; 
      background: #1e1e1e; 
      width: 300px; 
      height: 300px; 
      margin: 20px auto; 
      overflow-y: auto; 
      padding: 10px; 
      border-radius: 10px; 
      text-align: left;
    }
    input, button { 
      padding: 8px; 
      border-radius: 5px; 
      border: none; 
      margin-top: 5px; 
    }
    input { width: 200px; background: #2a2a2a; color: white; }
    button { background: #4caf50; color: white; cursor: pointer; }
    button:hover { background: #45a049; }
  </style>
</head>
<body>
  <header>
    <img src="https://i.pinimg.com/originals/5c/e1/30/5ce1309b98c812e6a539c116043e2faa.gif" alt="Chatbot Logo">
    <h2>My AI Chatbot</h2>
  </header>

  <div id="chat"></div>
  <input id="input" placeholder="Type a message..." onkeydown="if(event.key==='Enter'){sendMessage()}">
  <button onclick="sendMessage()">Send</button>

  <script>
    function sendMessage() {
      let input = document.getElementById("input");
      let chat = document.getElementById("chat");
      let userText = input.value.trim();
      if (!userText) return;

      chat.innerHTML += "<p><b>You:</b> " + userText + "</p>";
      input.value = "";

      // Simple bot replies
      let botReply = "I don’t understand.";
      if (userText.toLowerCase().includes("hello")) botReply = "Hi there!";
      if (userText.toLowerCase().includes("how are you")) botReply = "I'm just code, but I'm good!";
      if (userText.toLowerCase().includes("bye")) botReply = "Goodbye!";

      chat.innerHTML += "<p><b>Bot:</b> " + botReply + "</p>";
      chat.scrollTop = chat.scrollHeight;
    }
  </script>
</body>
</html>
